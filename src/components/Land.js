import React from 'react';
import styled, { keyframes } from 'styled-components';

const Land = ({ dark }) => {

    return (
        <div>
            this is the landing page
        </div>
    )
}

export default Land;